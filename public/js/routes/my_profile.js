/**
 *  @file Provides behaviour for the profile (your profile) page. Added to
 *  routing by {@link js/router.js}.
 */

/**
 *  Provides behaviour for the profile (your profile) page. Added to
 *  routing by {@link js/router.js}.
 */
function MyProfilePage() {

  /** Called on page load by Satnav. */
  this.route = function(params) {
    
  }

  return this;
}

var myProfilePage = new MyProfilePage();



